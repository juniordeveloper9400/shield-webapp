{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": null,
  "installCommand": "echo \"skip install\"",
  "buildCommand": "git clone https://github.com/flutter/flutter.git --depth 1 -b stable _flutter && cp lib/data/neon/neon_secret.example.dart lib/data/neon/neon_secret.dart && _flutter/bin/flutter config --enable-web && _flutter/bin/flutter pub get && _flutter/bin/flutter build web --release",
  "outputDirectory": "build/web",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
